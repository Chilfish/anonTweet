# 2026-09-06

## 可安装 PWA + Web Share Target（系统分享直达推文/IG）

**方案**：[docs/features/pwa/web-share-target.md](../features/pwa/web-share-target.md) | **AC**：[verify/acceptance-criteria/AC-pwa.md](../../verify/acceptance-criteria/AC-pwa.md)（AC-PWA-001/002/003）

**背景**：anonTweet 此前无任何 PWA 基建（无 manifest / 无 service worker）。目标是让站点在
Chrome Android 可安装，并作为 Web Share Target 接收系统分享——其它 App 分享 X/IG 链接时选择
Anon Tweet，首页自动预填并提交直达对应内容；不可识别文本则留框报错（与手动粘贴一致）。

**实现要点**：

- 接收点 = **首页 "/"**：`share_target` 用 GET 指向 `/`，不造中转页，复用 `TweetInputForm` 提交逻辑。
- 纯函数拆分：URL 检测 `extractTweetId/extractIGId/detectInputType` 自 `utils.ts` 抽到
  `app/lib/url-detect.ts`（utils 顶层 import UI/toast，纯单测/SSR 不该为它牵连 DOM 链）；
  `utils.ts` re-export 保持既有调用点。新增 `app/lib/share.ts`（pickSharedInput / resolveShareTarget /
  hasSharedContent）。`TweetInputForm` 提交改经 `resolveShareTarget`，加一次性分享落地 effect（ref 守卫）。
- PWA 基建（极简、无新运行时依赖）：`public/manifest.webmanifest`（含 share_target）；由
  `public/icon.webp` 经 magick 生成 512/192 方形 PNG；`public/sw.js` 极简网络透传 SW（同源 GET
  回源、绝不写 Cache、异源/非 GET 交默认，`skipWaiting`+`clients.claim`）；`app/lib/pwa/register.ts`
  仅生产注册；`root.tsx` 加 `<link rel="manifest">` + theme-color + 注册 effect；`vercel.json` 补 manifest
  Content-Type。
- 不用 vite-plugin-pwa/Workbox：SSR 的 HTML 由运行时产出、其 HTML 注入不适用；离线 precache 与
  serverless 导航冲突；以 ~15 行 SW 满足安装判定，零依赖零集成风险。

**门禁**：AC-PWA-001/002（静态，`test/acceptance/ac-pwa.spec.ts`）+ AC-PWA-003（语义，`test/unit/share.spec.ts`）。
真机「安装 + 系统分享」沙箱无设备，已在 AC 文档化交由部署者验证（域名 https://anon-tweet.chilfish.top/）。

**遗留（视觉项，需所有者验收）**：图标为 icon.webp 生成的透明方形 PNG，`theme_color`/maskable
变体观感待微调（仓库惯例）。

## PWA 能力盘点（DevTools 截图对照，capability-audit.md）

**背景**：owner 提供 Application > Service Workers 截图（#6900 activated & running）+ PWA 能力清单，
问「还有哪些未发掘场景」。产出 `docs/features/pwa/capability-audit.md`。

**线上核查（23:1x 直连 https://anon-tweet.chilfish.top/）**：sw.js（993B）与 manifest 均与仓库同源、
`manifest.webmanifest` Content-Type 正确；`<head>` 有 manifest link + theme-color，缺 iOS meta；
manifest 缺 maskable/screenshots/shortcuts。部署同步、SW 健康。

**裁决要点**：不做 = 离线 App 内容/IndexedDB 缓存/Background(Periodic) Sync/Web Push/Badging/Payment/
BT-USB-Serial/Wake Lock/收文件分享（无事件源、匿名无账号、SSR 回源铁律）；延后 = 静态资源缓存（需 ADR
修订 AC-PWA-002）。候选：P0 iOS head meta + maskable（收尾）；P1 share 外发（navigator.share，与现有
share_target 对称）+ manifest shortcuts/screenshots；P2 showSaveFilePicker / 最近查看。待 owner 裁决后
排期（backlog 已更新）。

## PWA 场景落地（owner 选定 3 项：iOS 外壳 / shortcuts+screenshots / share 出向）

**AC**：[AC-pwa.md v1.1](../../verify/acceptance-criteria/AC-pwa.md)（新增 AC-PWA-004/005/006）。

- **iOS 安装外壳（AC-PWA-004）**：`app/root.tsx` `<head>` 补 `apple-touch-icon`（sizes 180）+
  `mobile-web-app-capable` / `apple-mobile-web-app-capable` / `status-bar-style` / `apple-mobile-web-app-title`。
  资产 `public/icons/apple-touch-icon.png`（180x180 白底不透明）由 pwa-512 生成：
  `magick pwa-512x512.png -resize 180x180 -background white -alpha remove -alpha off apple-touch-icon.png`。
- **manifest shortcuts + screenshots（AC-PWA-005）**：`public/manifest.webmanifest` 增
  `shortcuts`（搜索推文 → `/search`，复用 192 图标）与 `screenshots`（wide 1280x800 / narrow 390x844）。
  截图是真实页面：Playwright chromium 直连线上首页抓取（脚本 `tmp/capture-pwa-shots.mjs`，不入库；
  chromium 取 `%LOCALAPPDATA%\ms-playwright` 最新目录的 executablePath）。
- **share 出向（AC-PWA-006）**：`app/lib/share.ts` 增 `buildTweetSharePayload` / `buildIGSharePayload` /
  `canNativeShare` / `shareOut`（navigator.share，用户取消静默；无 API 降级复制 url）；
  `use-tweet-operations`（shareTweet，materialize 后取主推文）、`use-ig-operations`（share，
  captionTranslation 优先）；两处 OptionsMenu 加「分享」（Share2）；IG 路由/IGHeader/stories 接线。
  降级 toast「已复制原文链接（当前浏览器不支持系统分享）」。
- **share 截图（AC-PWA-007，Web Share Level 2 files）**：owner 追问「分享能不能带图」→ 三个菜单入口
  方案选「独立『分享截图』项」。`app/lib/share.ts` 增 `dataUrlToFile`（dataURL→File）/ `canShareFiles`
  （`navigator.canShare({files})` 探测，Node/Firefox false）/ `shareImageOut`（AbortError 静默，
  `'unsupported'` 供回退）；两个截图 hook 的管线抽 `runCapture(mode)`，`mode='share'` 时把卡片图转
  File 系统分享，不支持/失败回退 `a[download]` 保存；推文与 IG 三点菜单各加「分享截图」（ImagePlus，
  截图中禁用防并发）。
- **版本更新提示（AC-PWA-008，优雅更新）**：owner 问「已安装用户如何优雅版本更新」→ 选「提示刷新」。
  因 SW 零缓存/SSR 回源，无旧 HTML 滞留，唯一缺口=已打开窗口需 reload 才见新 bundle。新增
  `app/lib/pwa/update.ts`（可见/聚焦 10min 节流 `registration.update()`；`updatefound`→`installing`
  statechange；`shouldOfferUpdate` 仅在**受控且有新 worker 接管**时提示，首次访问不打扰，单会话去重）
  + `app/components/pwa/UpdateNotice.tsx`（底部横幅「发现新版本·立即刷新」，稍后可隐藏；reload 前对
  waiting worker `postMessage({type:'SKIP_WAITING'})`），挂载 root `<body>`（SSR null）；`sw.js` 补
  message→skipWaiting 通道（仍零缓存）。
- **门禁**：typecheck / lint（仅改动文件，避免 eslint 重排全仓 md）/ test 366 / `verify --module pwa
  --exit-on-fail`（44）全绿。
- **提交**：`6a49f8a` feat(pwa) iOS+manifest；`807660b` feat(share) share-out；`cfd84c3` feat(share)
  share 截图；`63ce84b` feat(pwa) 版本更新提示；`f776c3d`/`2d84c59` docs AC；`9ac167a`/`e954616`/
  `d22ec18`/`3710c38` docs。
- **待 owner**：真机验证（安装 / share 进出 / share 截图发聊天 / 更新横幅 / iOS 添加主屏幕 / 安装对话框
  截图）；maskable 图标与 apple-touch-icon 底色观感（视觉项）。
