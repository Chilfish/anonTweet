# 2026-08-31

## 推文搜索接入（feat/tweet-search）

按 docs-first 启动 Twitter/X 推文搜索：`/search` 页面 + BFF `/api/tweet/search`（返回
`EnrichedTweet[]`，格式同 `/api/tweet/get`，先不做分页）+ rettiwt-api 授权补缺。

**现状盘点**：rettiwt-api 底层搜索链路（`TweetRequests.search` / `TweetService.search` /
`Extractors.TWEET_SEARCH` / `Requests.TWEET_SEARCH` / `FetchResourcesGroup`）已完整，缺口在：

1. `AllowGuestAuthenticationGroup` 缺 `TWEET_SEARCH`（guest 池直接 `RESOURCE_NOT_ALLOWED`）
2. App 数据层无搜索 fetch/解析函数
3. 无 BFF 路由 / `/search` 页面 / footer 入口

**计划**：

- 方案文档 `docs/features/tweet/search.md`（含接口约定与 5 阶段计划）
- AC 先行：`verify/acceptance-criteria/AC-tweet.md` v1.1 新增 AC-TWEET-009（离线解析回归）
  / AC-TWEET-010（集成端点返回推文列表）
- 数据层 `parseSearchTimeline` 纯函数（对齐 `fetchListTweets` 解析范式，规避 postmortem #001）
  - `fetchSearchTweets`（`twitterPool.run` + `ResourceType.TWEET_SEARCH`）
- 校验 `app/lib/validations/search.ts`、路由 `app/routes/api/tweet/search.ts`、页面
  `app/routes/search.tsx`、footer 内链入口

**约定**：搜索端点返回分页推文列表 `{ tweets: EnrichedTweet[], nextCursor }`
（推文元素格式同 `/api/tweet/get`，分页形态同 `/api/tweet/replies`），
前端「加载更多」用 `nextCursor` 翻页。

## 推文搜索落地（✅ 已完成，2026-08-31）

实现 + 测试全部完成，7 个原子提交入 `feat/tweet-search`：

- **数据层**：`parseSearchTimeline` 纯函数（entryId `tweet-` 过滤、光标剔除、
  `TweetWithVisibilityResults` 自动解包、`getBottomCursor` 复用）+ `fetchSearchTweets`
  （池化 `TWEET_SEARCH`；`q` 原样透传 rawQuery → **X 高级搜索语法直接可用**，
  `(from:7KoWa) until:2025-12-20` 单测锁定不改写）。
- **BFF**：`GET /api/tweet/search?q=&type=&cursor=&count=`，zod 校验（缺 q → 400），
  返回 `{ tweets: EnrichedTweet[], nextCursor }`，上游异常结构化 5xx。
- **页面**：`/search`（noindex）+ footer「More」内链「推文搜索」；搜索框 + 热门/最新
  切换 + **高级搜索快捷插入面板**（from:/to:/#/min_faves:/min_retweets:/since:/
  until:/lang:/-/精确短语 10 个 chip）；结果去重 + 「加载更多」cursor 翻页；
  空结果/失败状态区分，不再误报「推文未找到」（首次实测踩坑：空态误用 TweetNotFound）。
- **测试**：fixture `test/fixtures/search/search-tweets.json`；单测 13 例
  （parse 5 + 路由 5 + TweetFilter 语法透传 2 + visibility 解包 1）；
  AC-TWEET-009（离线回归）+ AC-TWEET-010（集成，skipIf 无 Key）；api-client 增 `tweet.search`。
- **门禁**：`bun run typecheck` ✅ / `bun run lint` 0 error ✅ / `bun run test` 294/294 ✅ /
  `bun run verify/index.ts --exit-on-fail` 301 passed + 5 skipped（需 TWEET_KEYS）✅。

## llms.txt + openapi.json 接入（✅ 已完成，2026-08-31）

按 [llmstxt.org](https://llmstxt.org/) 规范为 AI 爬虫/代理提供结构化站点与 API 描述：

- **`GET /llms.txt`**（`app/routes/llms.txt.ts`，`text/markdown`）：H1 标题 →
  blockquote 简介 → `## ` 分区（Key Pages / Backend API）→ `- [文本](链接): 描述`
  子弹列表；纯 markdown 无 HTML/表格/代码块；链接全部相对路径（任意域名可用）。
- **`GET /openapi.json`**（`app/routes/openapi.json.ts`，`application/json`）：
  OpenAPI 3.1 全量规范，覆盖 `app/routes.ts` 中 `api/*` 全部 16 条路径
  （tweet get/list/replies/search/set、ig get/translate、user get/timeline、
  ai-test/ai-translation/ai-vision、proxy/image、bili-post），带路径参数、
  query 参数、请求体与响应描述。
- **共享构建器** `app/lib/llms.ts`：`buildLlmsTxt(baseUrl)` +
  `buildOpenApiDoc(baseUrl)`，路由只做序列化（服务端模块，不进客户端 bundle）。
  OpenAPI 响应 schema `$ref` 到 `components.schemas` 内带真实字段的 `EnrichedTweet` /
  `TweetUser` / `Entity` / `IGPost` / `AIVisionInfo` / `RawUser` 等定义；llms.txt 另含
  **Data Shapes** 小节，直接给出搜索/获取推文等常用接口的出入参形态（EnrichedTweet
  字段、nextCursor 分页、search 的 q/type/cursor/count 入参）。
- **测试**：`test/unit/llms.spec.ts` AC-LLMS-001（llms.txt 规范形状 + 全端点覆盖 +
  链接相对化）+ AC-LLMS-002（OpenAPI 3.1 结构 + 路径与端点清单奇偶校验 +
  关键端点参数/请求体齐全）。
- 端点清单变更时同步改 `app/lib/llms.ts`，奇偶校验测试会兜底。
- **门禁**：`bun run typecheck` ✅ / `bun run lint` 0 error ✅ / `bun run test` ✅。

## anon-tweet Agent Skill 发布（/skills/anon-tweet/SKILL.md）

按 [Agent Skills 开放标准](https://agentskills.io)（spec：frontmatter 六个可移植字段
`name` / `description` / `license` / `compatibility` / `metadata` / `allowed-tools`，
`description` 首句为关键用途、body 单文件 <500 行）把仓库 API 使用指南整理为可分发 Skill：

- **本体**：`.agents/skills/anon-tweet/SKILL.md`（单一事实源，同时成为仓库项目 skill，
  随 `.agents/skills` 被 Claude Code 等代理自动发现）
- **发布链路**：`public/skills/anon-tweet` → 相对软链 `../../.agents/skills/anon-tweet`；
  Vite `copyDir`（`fs.statSync` 跟随软链）构建时以真实文件打进
  `build/client/skills/anon-tweet/`，线上 URL `/skills/anon-tweet/SKILL.md`
- **可发现性**：`/llms.txt` Machine Readable 小节新增链接子弹
- **git 可移植性实测**：`git add` 记录为 mode `120000`、blob 为相对目标
  `../../.agents/skills/anon-tweet`（Linux/Vercel checkout 为真软链，构建解引用）
- **验证**：`bun run build` ✅（`build/client/skills/anon-tweet/SKILL.md` 5420B 真实文件）、
  llms 单测 16/16 ✅、eslint 改动文件 ✅

**踩坑**：Windows 上 `core.symlinks=false` 的 git checkout 会把软链物化成普通文件
（内容为目标路径字符串），本地构建产物将缺失 SKILL.md——CI/Vercel（Linux）不受影响；
本机已有开发者模式可建软链。Windows 本地开发若缺文件：开开发者模式或
`git config core.symlinks true` 后重新 checkout，或本地重建软链
（`New-Item -ItemType SymbolicLink -Path public\skills\anon-tweet -Target ..\..\.agents\skills\anon-tweet`）。
