# 2026-08-31

## 推文搜索接入（feat/tweet-search）

按 docs-first 启动 Twitter/X 推文搜索：`/search` 页面 + BFF `/api/tweet/search`（返回
`EnrichedTweet[]`，格式同 `/api/tweet/get`，先不做分页）+ rettiwt-api 授权补缺。

**现状盘点**：rettiwt-api 底层搜索链路（`TweetRequests.search` / `TweetService.search` /
`Extractors.TWEET_SEARCH` / `Requests.TWEET_SEARCH` / `FetchResourcesGroup`）已完整，缺口在：

1. `AllowGuestAuthenticationGroup` 缺 `TWEET_SEARCH`（guest 池直接 `RESOURCE_NOT_ALLOWED`）
2. App 数据层无搜索 fetch/解析函数
3. 无 BFF 路由 / `/search` 页面 / footer 入口

**计划**：

- 方案文档 `docs/features/tweet/search.md`（含接口约定与 5 阶段计划）
- AC 先行：`verify/acceptance-criteria/AC-tweet.md` v1.1 新增 AC-TWEET-009（离线解析回归）
  / AC-TWEET-010（集成端点返回推文列表）
- 数据层 `parseSearchTimeline` 纯函数（对齐 `fetchListTweets` 解析范式，规避 postmortem #001）
  - `fetchSearchTweets`（`twitterPool.run` + `ResourceType.TWEET_SEARCH`）
- 校验 `app/lib/validations/search.ts`、路由 `app/routes/api/tweet/search.ts`、页面
  `app/routes/search.tsx`、footer 内链入口

**约定**：搜索端点返回分页推文列表 `{ tweets: EnrichedTweet[], nextCursor }`
（推文元素格式同 `/api/tweet/get`，分页形态同 `/api/tweet/replies`），
前端「加载更多」用 `nextCursor` 翻页。

## 推文搜索落地（✅ 已完成，2026-08-31）

实现 + 测试全部完成，7 个原子提交入 `feat/tweet-search`：

- **数据层**：`parseSearchTimeline` 纯函数（entryId `tweet-` 过滤、光标剔除、
  `TweetWithVisibilityResults` 自动解包、`getBottomCursor` 复用）+ `fetchSearchTweets`
  （池化 `TWEET_SEARCH`；`q` 原样透传 rawQuery → **X 高级搜索语法直接可用**，
  `(from:7KoWa) until:2025-12-20` 单测锁定不改写）。
- **BFF**：`GET /api/tweet/search?q=&type=&cursor=&count=`，zod 校验（缺 q → 400），
  返回 `{ tweets: EnrichedTweet[], nextCursor }`，上游异常结构化 5xx。
- **页面**：`/search`（noindex）+ footer「More」内链「推文搜索」；搜索框 + 热门/最新
  切换 + **高级搜索快捷插入面板**（from:/to:/#/min_faves:/min_retweets:/since:/
  until:/lang:/-/精确短语 10 个 chip）；结果去重 + 「加载更多」cursor 翻页；
  空结果/失败状态区分，不再误报「推文未找到」（首次实测踩坑：空态误用 TweetNotFound）。
- **测试**：fixture `test/fixtures/search/search-tweets.json`；单测 13 例
  （parse 5 + 路由 5 + TweetFilter 语法透传 2 + visibility 解包 1）；
  AC-TWEET-009（离线回归）+ AC-TWEET-010（集成，skipIf 无 Key）；api-client 增 `tweet.search`。
- **门禁**：`bun run typecheck` ✅ / `bun run lint` 0 error ✅ / `bun run test` 294/294 ✅ /
  `bun run verify/index.ts --exit-on-fail` 301 passed + 5 skipped（需 TWEET_KEYS）✅。
