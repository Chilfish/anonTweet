# 2026-08-09

## 工程规范对标 Float（CLAUDE.md 真实化 + 文档体系重建）

- **CLAUDE.md 真实化**：根因是 git 索引里 `CLAUDE.md` 是符号链接（mode `120000`，指向 `AGENTS.md`），Windows `core.symlinks=false` 下被解成普通文件但内容只是 `AGENTS.md` 一行。用 `git update-index --cacheinfo 100644,<blob>,CLAUDE.md` 强制改为普通文件，写入对标 Float 的完整规范（Project Context / Essential Commands / 结构 / Key Conventions / 🔴 强制规范 / Current State / Git Hooks / Skills）
- **AGENTS.md 弃用**：11KB 技术内容已由 `docs/` 承载 + 并入 CLAUDE.md，`git rm` 删除（决策：完全对标 Float——它只有 CLAUDE.md）
- **postmortem 迁移**：根目录 `postmortem/`（8 份报告 + index）→ `docs/postmortem/`（对齐 Float）。`index.md` 重写为 Float 风格 README（blameless 原则 + 索引表 + 高危文件 + 高频雷区自查 + Pre-Release 检查），新增 `TEMPLATE.md`（对标 Float 模板）
- **文档体系**：
  - `docs/README.md` — 文档索引（架构/验证/规划/工程规范/部署/记录/存档分类，对标 Float）
  - `docs/engineering/code-style.md` — TS/React/Zustand 规范 + 禁止事项 + 测试规范
  - `docs/engineering/git-workflow.md` — 分支模型 + Commit 纪律 + PR 流程 + 版本发布 + AI 协作
  - `docs/engineering/release-checklist.md` — 本地门禁 + 功能冒烟 + 验证套件
  - `docs/planning/action-plan.md` — 里程碑 + 当前阶段（verify 二期）
  - `docs/planning/architecture.md` — ADR-001~008（BFF / RettiwtPool / 双 Provider / Zustand / placeholder / 三层缓存 / verify / 媒体统一）
  - `docs/development-log/` — 按天开发日志（对标 Float）
- **CHANGELOG.md / CONTRIBUTING.md**：补齐（此前缺失）
- **lefthook 强化**：pre-push 从 `echo bun run build` 占位 → 真实 gate（typecheck + lint + test + verify）
- **断链修复**：AGENTS.md 引用的 `docs/SKILL/zustand-state-management.md` 不存在（该引用已随 AGENTS.md 删除；Zustand 规范并入 `docs/engineering/code-style.md`）

## 踩坑

- Windows `core.symlinks=false` 下，符号链接文件在 git 索引仍是 `120000`，`git add` 不会自动转普通文件——必须 `git update-index --cacheinfo 100644,<blob>,<path>` 显式改模式
- `docs/postmortem/` 迁移时，报告间链接是同目录文件名，无需改；但跨目录引用（`docs/verification-gap-analysis.md`、`docs/next-steps.md`）需更新 `postmortem/` → `docs/postmortem/`
