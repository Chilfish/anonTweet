# 2026-08-09

## 工程规范对标 Float（CLAUDE.md 真实化 + 文档体系重建）

- **CLAUDE.md 真实化**：根因是 git 索引里 `CLAUDE.md` 是符号链接（mode `120000`，指向 `AGENTS.md`），Windows `core.symlinks=false` 下被解成普通文件但内容只是 `AGENTS.md` 一行。用 `git update-index --cacheinfo 100644,<blob>,CLAUDE.md` 强制改为普通文件，写入对标 Float 的完整规范（Project Context / Essential Commands / 结构 / Key Conventions / 🔴 强制规范 / Current State / Git Hooks / Skills）
- **AGENTS.md 弃用**：11KB 技术内容已由 `docs/` 承载 + 并入 CLAUDE.md，`git rm` 删除（决策：完全对标 Float——它只有 CLAUDE.md）
- **postmortem 迁移**：根目录 `postmortem/`（8 份报告 + index）→ `docs/postmortem/`（对齐 Float）。`index.md` 重写为 Float 风格 README（blameless 原则 + 索引表 + 高危文件 + 高频雷区自查 + Pre-Release 检查），新增 `TEMPLATE.md`（对标 Float 模板）
- **文档体系**：
  - `docs/README.md` — 文档索引（架构/验证/规划/工程规范/部署/记录/存档分类，对标 Float）
  - `docs/engineering/code-style.md` — TS/React/Zustand 规范 + 禁止事项 + 测试规范
  - `docs/engineering/git-workflow.md` — 分支模型 + Commit 纪律 + PR 流程 + 版本发布 + AI 协作
  - `docs/engineering/release-checklist.md` — 本地门禁 + 功能冒烟 + 验证套件
  - `docs/planning/action-plan.md` — 里程碑 + 当前阶段（verify 二期）
  - `docs/planning/architecture.md` — ADR-001~008（BFF / RettiwtPool / 双 Provider / Zustand / placeholder / 三层缓存 / verify / 媒体统一）
  - `docs/development-log/` — 按天开发日志（对标 Float）
- **CHANGELOG.md / CONTRIBUTING.md**：补齐（此前缺失）
- **lefthook 强化**：pre-push 从 `echo bun run build` 占位 → 真实 gate（typecheck + lint + test + verify）
- **断链修复**：AGENTS.md 引用的 `docs/SKILL/zustand-state-management.md` 不存在（该引用已随 AGENTS.md 删除；Zustand 规范并入 `docs/engineering/code-style.md`）

## 踩坑

- Windows `core.symlinks=false` 下，符号链接文件在 git 索引仍是 `120000`，`git add` 不会自动转普通文件——必须 `git update-index --cacheinfo 100644,<blob>,<path>` 显式改模式
- `docs/postmortem/` 迁移时，报告间链接是同目录文件名，无需改；但跨目录引用（`docs/verification-gap-analysis.md`、`docs/next-steps.md`）需更新 `postmortem/` → `docs/postmortem/`

## 验证套件修复收尾 + Phase 2 启动

- 提交遗留修复 `fix(verify)`（2bf8af4）：test 7 fail → **44/44 全绿**；verify ~20 TS errors → **17 PASS / 0 FAIL / 3 SKIP**
  - **`bun test` 陷阱**：直接 `bun test` 触发 Bun 原生 runner（非 npm script），`vi.resetModules` 等 vitest API 不可用、模块解析不同 → 必须 `bun run test` 才走 vitest；lefthook pre-push 已同步修正
- AI SDK v7 迁移：`experimental_output` → `output`；实体字段 `translation` → `aiTranslation`（test 同步，verify 17 PASS 依赖此）
- `env.server.ts` 支持 `DOTENV_CONFIG_PATH`：测试环境指向缺失文件，避免真实 `.env` override 覆盖测试注入的 env
- ESLint 豁免 `rettiwt-api`（内置第三方逆向库，`chore(eslint)` fdda69a）：跨行 `prefer-regex-literals` 无法安全 autofix，忽略目录避免语义变更
- 启动 **Phase 2 S5（CI/CD Pipeline）**：文档先行 → 先更新本日志 + action-plan，再写 AC 与 workflow

## Phase 2 S5 完成 + S6 Screenshot Verifier 启动

- **S5 CI/CD Pipeline 完成**（`e10e727`）：`.github/workflows/verify.yml`（push + pull_request 触发、5 步：install → typecheck → lint → test → verify）+ `verify/modules/ci.verifier.ts`（CIVerifier 覆盖 AC-CI-001~004 静态检查）
- 收尾提交：action-plan 表格对齐（`b75af83`）+ ci.verifier 提取 `pushTriggerRe` 常量（`4dc921c`）
- **S6 Screenshot Verifier 启动**：文档先行 → 修正 AC-screenshot.md 端点路径（见下），再实现 `screenshot.verifier.ts`
- **踩坑：AC 端点路径与真实路由不一致**。`AC-screenshot.md` 写的 `GET /plain?tweetId=` / `/plain-ig?igId=` 与实际 `app/routes.ts` 的 `plain-tweet/:id` / `plain-ins/:id` 不符——截图路由历史上从 `/plain` 改名为 `/plain-tweet`，但 2026-07-04 编写 AC 时沿用了旧路径。已修正 AC 文档（v1.1）对齐 `routes.ts`，verifier 以真实端点为准

## Phase 2 S8 完成：服务器自动管理（`--server`）

- **接线**：`verify/index.ts` 的 `--server` 从占位改为真正启动/停止：`TestServer.start()` → `AnonTweetClient` 注入 ctx → `try/finally` 保证停止；`VerifyRunner.run(client?)` 支持外部注入
- **端口修复**：`vite.config.ts` `server.port` 硬编码 9080 不读 `PORT` env（TestServer 传 `PORT=9081` 无效）→ 改为 `Number(process.env.PORT) || 9080`
- **TestServer 增强**：端口复用探测（已有服务 → `Reusing` 不 spawn，结束时保留非自启进程）、Windows 进程树清理、`process.on('exit')` 兜底、`isolateExternal` 隔离外部 API key
- **验收**：`bun run verify/index.ts --server` → **26 PASS / 0 FAIL / 2 SKIP**（激活 AC-SHOT-001/002 + AC-TWEET-006；AC-TWEET-005/008 需真实 `TWEET_KEYS` 仍 SKIP），服务器自动停止、9081 端口释放

### 踩坑

- **Windows `child.kill('SIGTERM')` 不杀进程树**：dev server（vite worker 等）残留持有 stdio pipe，verify 主进程退出后管道不关 → CLI 挂住超时。改用 `spawnSync('taskkill', ['/PID', pid, '/T', '/F'])` 同步杀整个进程树
- **`env.server.ts` dotenv `override: true` + zod `.min(1)`**：隔离外部 key 时把 `GEMINI_API_KEY`/`DEEPSEEK_API_KEY` 置空串会触发 schema 校验失败（`❌ Invalid environment variables` → dev server 崩）。正确隔离 = `DOTENV_CONFIG_PATH` 指向缺失文件阻止 `.env` 加载（override:true 无法用空串覆盖）+ `INS_COOKIES`/`TWEET_KEYS` 空串（schema 允许）+ GEMINI/DEEPSEEK 用 `delete`（留 undefined）
- **AC-SHOT-002 确定性**：本地 `.env` 配了 `INS_COOKIES` 时，dev server 对 bogus id 也会走真实 IG 请求（网络不可达 → 降级渲染 NotFound → 内容断言失败）。`--server` 隔离后 `hasInsCookies=false` → 走 bogus id 确定性路径（渲染 HTML 即 PASS）
